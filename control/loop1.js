// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
// }

// for ([initialExpression/decleration] ; [condition]; [changer])
// {
//     //body

// }

let userScore = 10;
for ( userScore ; userScore >= 0; userScore--)
{
    if (userScore == 5)
    {
        continue
    }
console.log(`userscore is ${userScore}`);
}